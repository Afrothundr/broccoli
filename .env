# This env file should be checked into source control
# This is the place for default values for all environments
# Values in `.env.local` and `.env.production` will override these values
DATABASE_URL=fake
NEXT_PUBLIC_SCHEDULER_API_URL=http://localhost:5007
NEXT_PUBLIC_SCHEDULER_API_KEY=fake
UPLOADTHING_SECRET=sfake
UPLOADTHING_APP_ID=fake
UPLOADTHING_URL=http://localhost:3001
RESEND_API_KEY=fake
