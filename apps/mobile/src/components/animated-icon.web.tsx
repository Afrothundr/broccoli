// Web renders no launch overlay: there is no native splash to hand off from,
// so the page just paints. (The native variant fades out the splash surface.)
export function AnimatedSplashOverlay() {
  return null;
}
